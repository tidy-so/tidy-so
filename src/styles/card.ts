import type { ComponentSingleStyleConfig } from '@chakra-ui/theme';

const Card: ComponentSingleStyleConfig = {
  baseStyle: {
    p: 6,
    borderRadius: '3xl',
    shadow: 'xl',
    background: 'white',
    borderWidth: 2,
  },
};

export default Card;
